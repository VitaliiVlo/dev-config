# btop configuration

# Theme (closest to Catppuccin Macchiato from bundled themes)
color_theme = "tokyo-night"

# Transparent background (use terminal background)
theme_background = false

# Keep config minimal (don't expand with defaults on exit)
save_config_on_exit = false
