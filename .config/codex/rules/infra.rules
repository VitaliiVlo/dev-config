# Docker read-only
prefix_rule(pattern=["docker", ["ps", "images", "logs"]], decision="allow")
prefix_rule(pattern=["docker", "compose", ["ps", "config", "logs"]], decision="allow")

# Kubernetes read-only
prefix_rule(pattern=["kubectl", ["get", "describe", "logs"]], decision="allow")
